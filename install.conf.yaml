- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.zshrc: config/zshrc
    ~/.config/nvim: config/nvim/

- shell:
    - [git submodule update --init --recursive, Installing submodules]
    - [./copy-to-windows.sh, Copying Windows dotfiles]
